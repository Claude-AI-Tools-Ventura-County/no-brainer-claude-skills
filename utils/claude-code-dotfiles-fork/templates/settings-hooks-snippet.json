{
  "_comment": "Merge this `hooks` block into ~/.claude/settings.json. If you already have SessionStart/SessionEnd entries, append these objects to the existing arrays instead of replacing them. $HOME is expanded by the shell at hook runtime.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/.claude/hooks/session-start-sync.sh\""
          }
        ]
      }
    ],
    "SessionEnd": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "\"$HOME/.claude/hooks/session-end-sync.sh\""
          }
        ]
      }
    ]
  }
}
